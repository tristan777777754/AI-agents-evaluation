export default function HealthPage() {
  return (
    <main style={{ padding: "2rem", maxWidth: "48rem", margin: "0 auto" }}>
      <h1>Frontend Health</h1>
      <p>
        Phase 9 frontend surfaces are wired for backend-backed calibration, dataset, run, compare,
        review, and contract workflows.
      </p>
    </main>
  );
}
