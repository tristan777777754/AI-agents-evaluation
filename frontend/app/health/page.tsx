export default function HealthPage() {
  return (
    <main style={{ padding: "2rem", maxWidth: "48rem", margin: "0 auto" }}>
      <h1>Frontend Health</h1>
      <p>
        Phase 6 frontend surfaces are wired for real backend-backed dataset, run, compare, and
        review workflows.
      </p>
    </main>
  );
}
